import 'package:flutter/material.dart';

/// Colony App Color Palette
/// Premium dark theme with vibrant purple/pink accents
class AppColors {
  AppColors._();

  // ─── Primary Brand Colors ───────────────────────────────────
  static const Color colonyPurple = Color(0xFF7C3AED);
  static const Color colonyViolet = Color(0xFFA855F7);
  static const Color colonyPink = Color(0xFFEC4899);
  static const Color colonyBlue = Color(0xFF3B82F6);
  static const Color colonyTeal = Color(0xFF14B8A6);
  static const Color colonyAmber = Color(0xFFF59E0B);

  // ─── Extended Palette ───────────────────────────────────────
  static const Color colonyRed = Color(0xFFEF4444);
  static const Color colonyGreen = Color(0xFF22C55E);
  static const Color colonyIndigo = Color(0xFF6366F1);
  static const Color colonyCyan = Color(0xFF06B6D4);
  static const Color colonyOrange = Color(0xFFF97316);

  // ─── Background Colors (Dark theme primary) ─────────────────
  static const Color bgPrimary = Color(0xFF0A0A0F);
  static const Color bgSecondary = Color(0xFF12121A);
  static const Color bgTertiary = Color(0xFF1A1A2E);
  static const Color bgQuaternary = Color(0xFF16213E);

  // ─── Surface Colors ─────────────────────────────────────────
  static const Color surfaceLight = Color(0xFF1E1E2E);
  static const Color surfaceMedium = Color(0xFF252535);
  static const Color surfaceHigh = Color(0xFF2D2D42);

  // ─── Text Colors ────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0C4);
  static const Color textMuted = Color(0xFF6B6B80);
  static const Color textAccent = Color(0xFFA855F7);

  // ─── Semantic Colors ────────────────────────────────────────
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ─── Overlay Colors ─────────────────────────────────────────
  static const Color overlayDark = Color(0x80000000);
  static const Color overlayLight = Color(0x1AFFFFFF);
  static const Color overlayPurple = Color(0x407C3AED);
  static const Color overlayPink = Color(0x40EC4899);

  // ─── Border Colors ──────────────────────────────────────────
  static const Color borderLight = Color(0x1AFFFFFF);
  static const Color borderMedium = Color(0x33FFFFFF);
  static const Color borderDark = Color(0x0DFFFFFF);

  // ─── Shimmer Colors ─────────────────────────────────────────
  static const Color shimmerBase = Color(0xFF1E1E2E);
  static const Color shimmerHighlight = Color(0xFF2D2D42);

  // ─── Glass Effect Colors ────────────────────────────────────
  static const Color glassWhite = Color(0x0DFFFFFF);
  static const Color glassBorder = Color(0x1AFFFFFF);

  // ─── Status Colors ──────────────────────────────────────────
  static const Color online = Color(0xFF14B8A6);
  static const Color offline = Color(0xFF6B6B80);
  static const Color away = Color(0xFFF59E0B);
  static const Color busy = Color(0xFFEF4444);

  // ─── Coin/Premium Colors ────────────────────────────────────
  static const Color coinGold = Color(0xFFF59E0B);
  static const Color premiumPurple = Color(0xFF7C3AED);
  static const Color premiumPink = Color(0xFFEC4899);

  // ─── Story Ring Colors ──────────────────────────────────────
  static const Color storyRingStart = Color(0xFF7C3AED);
  static const Color storyRingEnd = Color(0xFFEC4899);
  static const Color storyRingSeen = Color(0xFF6B6B80);
}
